# چک‌لیست پیاده‌سازی Purple VOID PV-BPI 1.1

این چک‌لیست پیش از شروع کدنویسی و پس از مطالعه کامل و بازبینی تصویری هر ۲۰ صفحه دو سند مرجع تهیه شده است.

## منابع تغییرناپذیر

- [x] محتوای ۳۵ سؤال و تمام گزینه‌ها فقط از `Purple_VOID_35_Question_Test_FA_FIXED.pdf` خوانده شود و بدون بازنویسی، کوتاه‌سازی، جابه‌جایی یا اصلاح نگارشی در داده‌های برنامه قرار بگیرد.
- [x] تمام نگاشت‌ها، امتیازها، آستانه‌ها، قواعد کیفیت، اطمینان، رشد و سایه فقط از `Purple_VOID_PV-BPI_v1.1_Scoring_System_FA_FIXED.pdf` پیاده‌سازی شود.
- [x] هش SHA-256 فایل سؤال‌ها: `E41D78F2E50A30BB3D250A5E4D1731E724D297A64FD53C2CD00655CB14E4C9D6`.
- [x] هش SHA-256 فایل امتیازدهی: `67010177E2059DED189EC507AA1B9CCF12A9307FAAB8ED493C8C3B0AFADD92FA`.
- [x] نسخه تست `beta-1.1-BPI-PV` و نسخه الگوریتم `1.1` در داده ذخیره‌شده ثبت شود.

## داده آزمون

- [x] سؤال‌های ۱ تا ۳۰ با متن دقیق PDF و مقیاس دقیق `۱ اصلاً`، `۲ به ندرت`، `۳ بعضی وقت ها`، `۴ بیشتر وقت ها`، `۵ تقریباً همیشه` ثبت شود.
- [x] سؤال‌های ۳۱ تا ۳۵ با متن و پنج گزینه دقیق PDF و شناسه پایدار A تا E ثبت شود.
- [x] محورهای مستقیم، معکوس و سناریوی متناظر در پیکربندی تایپ‌شده و جدا از رابط کاربری ثبت شود.
- [x] امتیاز سناریوها دقیقاً به صورت جایگشت ۰ تا ۴ ثبت و شناسه خالی یا ناشناخته رد شود.
- [x] ترتیب دیداری گزینه‌ها هرگز مبنای امتیازدهی نباشد.

## موتور امتیازدهی خالص

- [x] پاسخ‌های ۳۰ لیکرت و ۵ شناسه سناریو پیش از محاسبه کامل اعتبارسنجی شود.
- [x] آیتم معکوس فقط با `6 - rawAnswer` هم‌جهت شود.
- [x] برای هر محور `LikertSum`، `ScenarioScore`، `RawAxis`، `RawIndex` و `AxisScore` بدون گردکردن میانی محاسبه شود.
- [x] تمام مقایسه‌ها فقط با `RawIndex` صحیح انجام شود و `AxisScore` فقط برای نمایش گرد شود.
- [x] سطح مهارت دقیقاً با بازه‌های خام ۰–۱۱، ۱۲–۱۶، ۱۷–۲۲ و ۲۳–۲۸ تعیین شود.
- [x] قواعد نوع نتیجه به ترتیب اولویت BALANCED، BLENDED، PRIMARY_SUPPORT و DOMINANT اجرا شود.
- [x] تساوی‌ها با ترتیب تصادفی سطح نشست شکسته و همان seed/ترتیب همراه نتیجه ذخیره شود.
- [x] پایین‌ترین محور مسیر رشد باشد؛ اختلاف حداکثر ۲ با محور یکی‌مانده‌به‌آخر هر دو را مسیر رشد کند.

## کیفیت و اطمینان

- [x] زمان کل کمتر از ۱۵۰ ثانیه به صورت نشانه ساخت‌یافته ثبت شود.
- [x] بیش از ۲۰٪ پاسخ‌های کمتر از ۲ ثانیه به صورت نشانه ساخت‌یافته ثبت شود.
- [x] توالی یکسان حداقل ۸ پاسخ یا استفاده از یک گزینه در حداقل ۷۰٪ سؤال‌های لیکرت تشخیص داده شود.
- [x] الگوی کاملاً ایدئال (۵ مستقیم و ۱ معکوس) تشخیص داده شود.
- [x] ناسازگاری حداقل ۳ امتیاز در دست‌کم ۲ جفت کنترل، پس از معکوس‌سازی آیتم منفی، تشخیص داده شود.
- [x] واگرایی `LikertIndex >= 19` و `ScenarioScore <= 1` در دست‌کم ۳ محور تشخیص داده شود.
- [x] حجم تجربه کمتر از ۸ معامله به صورت نشانه ساخت‌یافته ثبت شود.
- [x] سطح اطمینان معمول، متوسط یا پایین دقیقاً از ترکیب بازه معامله و تعداد نشانه‌های کیفیت تعیین شود.
- [x] برای اطمینان پایین متن استاندارد PDF با عبارت `حداقل ۱۵ معامله` نمایش داده و سایه حذف شود.

## منطق سایه

- [x] پنج قاعده RIGIDITY، ANALYSIS LOOP، STRATEGY HOPPING، AVOIDANT DISTANCE و OVERCONTROL دقیقاً روی RawIndex اجرا شود.
- [x] حداکثر یک سایه با بزرگ‌ترین فاصله محور بالا و پایین انتخاب شود.
- [x] در تساوی بزرگ‌ترین فاصله، شرط ناقص یا اطمینان پایین، هیچ سایه‌ای نمایش داده نشود.

## تجربه کاربری و ذخیره‌سازی

- [x] مسیرهای `/`، `/test`، `/test/questions`، `/results`، `/methodology` و `/privacy` ساخته شود.
- [x] تعداد معامله در چهار بازه و راهنمای یادآوری ۶۰/۹۰ روزه یا پروفایل فرضی نمایش داده شود.
- [x] پاسخ خام، اندیس سؤال، زمان شروع، زمان هر سؤال، بازه معامله، نسخه و tie-break در LocalStorage ذخیره شود.
- [x] پیشرفت پس از refresh ادامه پیدا کند و نتیجه پس از refresh ثابت بماند.
- [x] شروع دوباره با تأیید و پاک‌کردن کامل داده محلی فراهم شود.
- [x] نتیجه ناقص هرگز ساخته نشود.
- [x] رابط فارسی RTL، متن انگلیسی LTR، پشتیبانی کامل صفحه‌کلید، focus واضح، radio معنایی و هدف لمسی حداقل ۴۴px رعایت شود.
- [x] reduced motion، کنتراست، موبایل و دسکتاپ بررسی شود.

## خروجی نتیجه

- [x] نوع پروفایل، سطح اطمینان، آرکتایپ اصلی/دوم، پنج محور، سطح مهارت، مسیر رشد و حداکثر یک سایه نمایش داده شود.
- [x] نمودار پنج‌محوری امتیاز گرد‌شده و عدم قطعیت نمایشی تقریبی ±۱۰ را بدون ادعای دقت بیشتر نشان دهد.
- [x] تفسیر آموزشی، غیرتشخیصی و غیرفاتالیستی باشد.
- [x] توصیه عمومی بازآزمایی دقیقاً پس از ۳۰ روز یا حداقل ۲۰ معامله جدید باشد.
- [x] متن استاندارد محدودیت PDF بدون تغییر معنا نمایش داده شود.
- [x] ادعای شخصیت دائمی، تشخیص روان‌شناختی، سودآوری، تناسب سرمایه‌گذاری یا اعتبار بالینی وجود نداشته باشد.

## آزمون و تحویل

- [x] تست واحد برای همه نگاشت‌ها، دامنه‌ها، نوع نتیجه، رشد، کیفیت، اطمینان، سایه، داده ناقص و ماندگاری tie نوشته شود.
- [x] Playwright مسیر کامل ۳۵ سؤال و پایداری نتیجه پس از refresh را اجرا کند.
- [x] README، `.env.example`، سند معماری و نگاشت قواعد PDF به توابع نوشته شود.
- [x] lint، typecheck، unit، E2E و production build اجرا و تمام خطاها اصلاح شود.
